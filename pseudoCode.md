Functions not included

```
print welcome message
get roomCount input

for # of rooms:
    get shapeSelection input

    if shapeSelection == rectangle:
        get width, length, height input
        calc
    if shapeSelection == square:
        get length input
        calc
    if shapeSelection == custom:
        get wallCount, width, length, height input
        calc
    
    get windowDoorCount input

    for # of window/doors:
        get width, length input
        calc

    calc
    print surface area of room, paint volume for room, cost of room
    add values from previous loops

print surface area of house, paint volume of house, cost of house 
```